spring.datasource.url=jdbc:mysql://localhost:3306/db_temporary?serverTimezone=Europe/Rome
spring.datasource.username=beatrice
spring.datasource.password=

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

spring.mail.properties.mail.smtp.starttls.enable=true

#GMAIL SMTP CONFIG
spring.mail.host = smtp.gmail.com
spring.mail.port = 587
spring.mail.username = beatricebaldassarre86@gmail.com
spring.mail.password = e#6AXj8m94T!

# App Properties
thesis.jwtSecret= bisioSecretKey
thesis.jwtExpirationMs= 86400000
